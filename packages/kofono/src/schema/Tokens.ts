export enum Token {
    SchemaId = "$id",
    SchemaExtensions = "$extensions",
    SchemaVars = "$vars",
    SchemaTranslations = "$translations",
    DefaultValue = "default",
    Component = "component",
    Enum = "enum",
    Properties = "__",
    Qualifications = "$q",
    Validations = "$v",
}
